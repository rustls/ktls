name: test

on:
  push:
    branches: ["main"]
  pull_request:
  merge_group:

jobs:
  test:
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: Run tests
        run: |
          cd ${{ github.workspace }}
          cargo clippy
          just ci-test
